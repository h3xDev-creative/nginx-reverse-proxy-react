[defaults]
inventory = inventory/hosts   # Path to the inventory file
roles_path = ./roles          # Path to roles directory
remote_user = ubuntu          # Default user for SSH (modify based on your setup)
host_key_checking = False     # Disable host key checking (useful for dev environments)
